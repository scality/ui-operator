---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: scalityuicomponentexposers.ui.scality.com
spec:
  group: ui.scality.com
  names:
    kind: ScalityUIComponentExposer
    listKind: ScalityUIComponentExposerList
    plural: scalityuicomponentexposers
    singular: scalityuicomponentexposer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScalityUIComponentExposer is the Schema for the scalityuicomponentexposers
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScalityUIComponentExposerSpec defines the desired state of
              ScalityUIComponentExposer
            properties:
              scality-ui:
                description: |-
                  INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
              scality-ui-component:
                type: string
            required:
            - scality-ui
            - scality-ui-component
            type: object
          status:
            description: ScalityUIComponentExposerStatus defines the observed state
              of ScalityUIComponentExposer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
