---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: scalityuis.ui.scality.com
spec:
  group: ui.scality.com
  names:
    kind: ScalityUI
    listKind: ScalityUIList
    plural: scalityuis
    singular: scalityui
  scope: Namespaced
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ScalityUI is the Schema for the scalityuis API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: ScalityUISpec defines the desired state of ScalityUI
              properties:
                canChangeInstanceName:
                  type: boolean
                canChangeLanguage:
                  type: boolean
                canChangeTheme:
                  type: boolean
                discoveryURL:
                  type: string
                favicon:
                  type: string
                image:
                  description: |-
                    Specifies the container image for the ScalityUI application.
                    The value should be in the format `repository:tag`, e.g., `scality/ui:latest`.
                  type: string
                logo:
                  type: string
                mountPath:
                  type: string
                navbar:
                  description: Navbar configures the UI navbar.
                  properties:
                    main:
                      items:
                        description: NavbarItem defines an item in the navbar.
                        properties:
                          groups:
                            items:
                              type: string
                            type: array
                          icon:
                            type: string
                          isExternal:
                            type: boolean
                          kind:
                            type: string
                          label:
                            additionalProperties:
                              type: string
                            type: object
                          url:
                            type: string
                          view:
                            type: string
                        required:
                          - kind
                          - view
                        type: object
                      type: array
                    subLogin:
                      items:
                        description: NavbarItem defines an item in the navbar.
                        properties:
                          groups:
                            items:
                              type: string
                            type: array
                          icon:
                            type: string
                          isExternal:
                            type: boolean
                          kind:
                            type: string
                          label:
                            additionalProperties:
                              type: string
                            type: object
                          url:
                            type: string
                          view:
                            type: string
                        required:
                          - kind
                          - view
                        type: object
                      type: array
                  required:
                    - main
                    - subLogin
                  type: object
                networks:
                  description: UINetworks configures network parameters for the UI.
                  properties:
                    host:
                      type: string
                    ingressAnnotations:
                      additionalProperties:
                        type: string
                      type: object
                    ingressClassName:
                      type: string
                    tls:
                      items:
                        description:
                          IngressTLS defines the TLS configuration for the
                          ingress.
                        properties:
                          hosts:
                            items:
                              type: string
                            type: array
                          secretName:
                            type: string
                        type: object
                      type: array
                  required:
                    - host
                    - ingressClassName
                  type: object
                productName:
                  type: string
                themes:
                  description: Themes defines the various themes supported by the UI.
                  properties:
                    dark:
                      description: Theme defines a theme supported by the UI.
                      properties:
                        logoPath:
                          type: string
                        name:
                          type: string
                        type:
                          type: string
                      required:
                        - logoPath
                        - name
                        - type
                      type: object
                    light:
                      description: Theme defines a theme supported by the UI.
                      properties:
                        logoPath:
                          type: string
                        name:
                          type: string
                        type:
                          type: string
                      required:
                        - logoPath
                        - name
                        - type
                      type: object
                  required:
                    - dark
                    - light
                  type: object
              required:
                - image
                - productName
              type: object
            status:
              description: ScalityUIStatus defines the observed state of ScalityUI
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
