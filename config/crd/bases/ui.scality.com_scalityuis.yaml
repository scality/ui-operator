---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: scalityuis.ui.scality.com
spec:
  group: ui.scality.com
  names:
    kind: ScalityUI
    listKind: ScalityUIList
    plural: scalityuis
    singular: scalityui
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScalityUI is the Schema for the scalityuis API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScalityUISpec defines the desired state of ScalityUI
            properties:
              canChangeInstanceName:
                type: boolean
              canChangeLanguage:
                type: boolean
              canChangeTheme:
                type: boolean
              favicon:
                type: string
              image:
                description: |-
                  Image specifies the container image to be used for the Scality UI.
                  This should be a valid container image reference, including the tag.
                type: string
              logo:
                type: string
              navbar:
                description: Navbar configures the UI navbar.
                properties:
                  main:
                    description: Main contains configuration for the main navigation
                      items in the navbar.
                    items:
                      description: NavbarItem defines an item in the navbar.
                      properties:
                        groups:
                          description: |-
                            Groups contains a list of user groups that can see this navbar item.
                            If empty, the item is visible to all users.
                          items:
                            type: string
                          type: array
                        icon:
                          description: Icon is the name or path of the icon to display
                            for this navbar item.
                          type: string
                        isExternal:
                          description: IsExternal indicates whether the item links
                            to an external resource.
                          type: boolean
                        kind:
                          description: Kind specifies the type of navbar item.
                          type: string
                        label:
                          additionalProperties:
                            type: string
                          description: Label contains localized text for the navbar
                            item, keyed by language code.
                          type: object
                        url:
                          description: URL specifies the link destination when IsExternal
                            is true.
                          type: string
                        view:
                          description: View identifies the associated view for this
                            navbar item.
                          type: string
                      required:
                      - kind
                      - view
                      type: object
                    type: array
                  subLogin:
                    description: SubLogin contains configuration for navigation items
                      displayed in the sub-login area.
                    items:
                      description: NavbarItem defines an item in the navbar.
                      properties:
                        groups:
                          description: |-
                            Groups contains a list of user groups that can see this navbar item.
                            If empty, the item is visible to all users.
                          items:
                            type: string
                          type: array
                        icon:
                          description: Icon is the name or path of the icon to display
                            for this navbar item.
                          type: string
                        isExternal:
                          description: IsExternal indicates whether the item links
                            to an external resource.
                          type: boolean
                        kind:
                          description: Kind specifies the type of navbar item.
                          type: string
                        label:
                          additionalProperties:
                            type: string
                          description: Label contains localized text for the navbar
                            item, keyed by language code.
                          type: object
                        url:
                          description: URL specifies the link destination when IsExternal
                            is true.
                          type: string
                        view:
                          description: View identifies the associated view for this
                            navbar item.
                          type: string
                      required:
                      - kind
                      - view
                      type: object
                    type: array
                type: object
              networks:
                description: UINetworks configures network parameters for the UI.
                properties:
                  host:
                    description: Host specifies the hostname for the UI ingress.
                    type: string
                  ingressAnnotations:
                    additionalProperties:
                      type: string
                    description: IngressAnnotations provides custom annotations for
                      the ingress resource.
                    type: object
                  ingressClassName:
                    description: IngressClassName specifies which ingress controller
                      should implement the resource.
                    type: string
                  tls:
                    description: TLS configures the TLS settings for the ingress.
                    items:
                      description: IngressTLS defines the TLS configuration for the
                        ingress.
                      properties:
                        hosts:
                          description: Hosts is a list of hosts included in the TLS
                            certificate.
                          items:
                            type: string
                          type: array
                        secretName:
                          description: SecretName is the name of the secret used to
                            terminate TLS traffic.
                          type: string
                      type: object
                    type: array
                required:
                - host
                - ingressClassName
                type: object
              productName:
                type: string
              themes:
                description: Themes defines the various themes supported by the UI.
                properties:
                  dark:
                    description: Dark is the dark theme configuration for the UI.
                    properties:
                      logoPath:
                        description: LogoPath is the path to the logo image file to
                          use with this theme.
                        type: string
                      name:
                        description: Name is the unique identifier for the theme.
                        type: string
                      type:
                        description: Type specifies the theme's category or system
                          (e.g., 'core-ui').
                        type: string
                    required:
                    - logoPath
                    - name
                    - type
                    type: object
                  light:
                    description: Light is the light theme configuration for the UI.
                    properties:
                      logoPath:
                        description: LogoPath is the path to the logo image file to
                          use with this theme.
                        type: string
                      name:
                        description: Name is the unique identifier for the theme.
                        type: string
                      type:
                        description: Type specifies the theme's category or system
                          (e.g., 'core-ui').
                        type: string
                    required:
                    - logoPath
                    - name
                    - type
                    type: object
                required:
                - dark
                - light
                type: object
            required:
            - image
            - productName
            type: object
          status:
            description: ScalityUIStatus defines the observed state of ScalityUI
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
