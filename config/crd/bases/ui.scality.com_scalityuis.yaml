---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: scalityuis.ui.scality.com
spec:
  group: ui.scality.com
  names:
    kind: ScalityUI
    listKind: ScalityUIList
    plural: scalityuis
    singular: scalityui
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScalityUI is the Schema for the scalityuis API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScalityUISpec defines the desired state of ScalityUI
            properties:
              auth:
                description: AuthConfig defines authentication configuration
                properties:
                  clientId:
                    description: ClientID is the OIDC client ID
                    type: string
                  kind:
                    default: OIDC
                    description: Kind specifies the authentication type (e.g., "OIDC")
                    enum:
                    - OIDC
                    - Basic
                    - None
                    type: string
                  providerLogout:
                    default: true
                    description: ProviderLogout enables provider logout
                    type: boolean
                  providerUrl:
                    description: ProviderURL is the OIDC provider URL
                    type: string
                  redirectUrl:
                    default: /
                    description: RedirectURL is the redirect URL after authentication
                    type: string
                  responseType:
                    default: code
                    description: ResponseType specifies the OIDC response type
                    type: string
                  scopes:
                    default: openid email profile
                    description: Scopes specifies the OIDC scopes
                    type: string
                type: object
              image:
                description: |-
                  INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
              navbar:
                description: Navbar configures the UI navbar.
                properties:
                  main:
                    description: Main contains configuration for the main navigation
                      items in the navbar.
                    items:
                      description: NavbarItem defines an item in the navbar that can
                        be either internal or external.
                      properties:
                        external:
                          description: |-
                            External contains configuration for external navigation items.
                            Only one of Internal or External should be specified.
                          properties:
                            groups:
                              description: |-
                                Groups contains a list of user groups that can see this navbar item.
                                If empty, the item is visible to all users.
                              items:
                                type: string
                              type: array
                            icon:
                              description: Icon is the name or path of the icon to
                                display for this navbar item.
                              type: string
                            label:
                              additionalProperties:
                                type: string
                              description: Label contains localized text for the navbar
                                item, keyed by language code.
                              type: object
                            url:
                              description: URL specifies the link destination for
                                the external resource.
                              type: string
                          required:
                          - url
                          type: object
                        internal:
                          description: |-
                            Internal contains configuration for internal navigation items.
                            Only one of Internal or External should be specified.
                          properties:
                            groups:
                              description: |-
                                Groups contains a list of user groups that can see this navbar item.
                                If empty, the item is visible to all users.
                              items:
                                type: string
                              type: array
                            icon:
                              description: Icon is the name or path of the icon to
                                display for this navbar item.
                              type: string
                            kind:
                              description: Kind specifies the type of navbar item.
                              type: string
                            label:
                              additionalProperties:
                                type: string
                              description: Label contains localized text for the navbar
                                item, keyed by language code.
                              type: object
                            view:
                              description: View identifies the associated view for
                                this navbar item.
                              type: string
                          required:
                          - kind
                          - view
                          type: object
                      type: object
                    type: array
                  subLogin:
                    description: SubLogin contains configuration for navigation items
                      displayed in the sub-login area.
                    items:
                      description: NavbarItem defines an item in the navbar that can
                        be either internal or external.
                      properties:
                        external:
                          description: |-
                            External contains configuration for external navigation items.
                            Only one of Internal or External should be specified.
                          properties:
                            groups:
                              description: |-
                                Groups contains a list of user groups that can see this navbar item.
                                If empty, the item is visible to all users.
                              items:
                                type: string
                              type: array
                            icon:
                              description: Icon is the name or path of the icon to
                                display for this navbar item.
                              type: string
                            label:
                              additionalProperties:
                                type: string
                              description: Label contains localized text for the navbar
                                item, keyed by language code.
                              type: object
                            url:
                              description: URL specifies the link destination for
                                the external resource.
                              type: string
                          required:
                          - url
                          type: object
                        internal:
                          description: |-
                            Internal contains configuration for internal navigation items.
                            Only one of Internal or External should be specified.
                          properties:
                            groups:
                              description: |-
                                Groups contains a list of user groups that can see this navbar item.
                                If empty, the item is visible to all users.
                              items:
                                type: string
                              type: array
                            icon:
                              description: Icon is the name or path of the icon to
                                display for this navbar item.
                              type: string
                            kind:
                              description: Kind specifies the type of navbar item.
                              type: string
                            label:
                              additionalProperties:
                                type: string
                              description: Label contains localized text for the navbar
                                item, keyed by language code.
                              type: object
                            view:
                              description: View identifies the associated view for
                                this navbar item.
                              type: string
                          required:
                          - kind
                          - view
                          type: object
                      type: object
                    type: array
                type: object
              networks:
                description: UINetworks configures network parameters for the UI.
                properties:
                  host:
                    description: Host specifies the hostname for the UI ingress.
                    type: string
                  ingressAnnotations:
                    additionalProperties:
                      type: string
                    description: IngressAnnotations provides custom annotations for
                      the ingress resource.
                    type: object
                  ingressClassName:
                    description: IngressClassName specifies which ingress controller
                      should implement the resource.
                    type: string
                  tls:
                    description: TLS configures the TLS settings for the ingress.
                    items:
                      description: IngressTLS describes the transport layer security
                        associated with an ingress.
                      properties:
                        hosts:
                          description: |-
                            hosts is a list of hosts included in the TLS certificate. The values in
                            this list must match the name/s used in the tlsSecret. Defaults to the
                            wildcard host setting for the loadbalancer controller fulfilling this
                            Ingress, if left unspecified.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        secretName:
                          description: |-
                            secretName is the name of the secret used to terminate TLS traffic on
                            port 443. Field is left optional to allow TLS routing based on SNI
                            hostname alone. If the SNI host in a listener conflicts with the "Host"
                            header field used by an IngressRule, the SNI host is used for termination
                            and value of the "Host" header is used for routing.
                          type: string
                      type: object
                    type: array
                required:
                - host
                - ingressClassName
                type: object
              productName:
                type: string
              themes:
                description: Themes defines the various themes supported by the UI.
                properties:
                  dark:
                    description: Dark is the dark theme configuration for the UI.
                    properties:
                      logo:
                        description: Logo contains the logo configuration for this
                          theme.
                        properties:
                          mimeType:
                            description: |-
                              MimeType specifies the MIME type when using base64 (e.g., "image/png", "image/jpeg").
                              Optional for "path" and "svg" types.
                            type: string
                          type:
                            description: 'Type specifies the logo type: "path", "base64",
                              or "svg".'
                            type: string
                          value:
                            description: |-
                              Value contains the logo data based on the type:
                              - For "path": file path to the logo image
                              - For "base64": base64-encoded image data
                              - For "svg": inline SVG content
                            type: string
                        required:
                        - type
                        - value
                        type: object
                      name:
                        description: Name is the unique identifier for the theme.
                        type: string
                      type:
                        description: Type specifies the theme's category or system
                          (e.g., 'core-ui').
                        type: string
                    required:
                    - logo
                    - name
                    - type
                    type: object
                  light:
                    description: Light is the light theme configuration for the UI.
                    properties:
                      logo:
                        description: Logo contains the logo configuration for this
                          theme.
                        properties:
                          mimeType:
                            description: |-
                              MimeType specifies the MIME type when using base64 (e.g., "image/png", "image/jpeg").
                              Optional for "path" and "svg" types.
                            type: string
                          type:
                            description: 'Type specifies the logo type: "path", "base64",
                              or "svg".'
                            type: string
                          value:
                            description: |-
                              Value contains the logo data based on the type:
                              - For "path": file path to the logo image
                              - For "base64": base64-encoded image data
                              - For "svg": inline SVG content
                            type: string
                        required:
                        - type
                        - value
                        type: object
                      name:
                        description: Name is the unique identifier for the theme.
                        type: string
                      type:
                        description: Type specifies the theme's category or system
                          (e.g., 'core-ui').
                        type: string
                    required:
                    - logo
                    - name
                    - type
                    type: object
                required:
                - dark
                - light
                type: object
            required:
            - image
            - productName
            type: object
          status:
            description: ScalityUIStatus defines the observed state of ScalityUI
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
