---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: scalityuicomponentexposers.ui.scality.com
spec:
  group: ui.scality.com
  names:
    kind: ScalityUIComponentExposer
    listKind: ScalityUIComponentExposerList
    plural: scalityuicomponentexposers
    singular: scalityuicomponentexposer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScalityUIComponentExposer is the Schema for the scalityuicomponentexposers
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScalityUIComponentExposerSpec defines the desired state of
              ScalityUIComponentExposer
            properties:
              appHistoryBasePath:
                description: AppHistoryBasePath is the base path for app history
                type: string
              auth:
                description: Auth contains authentication configuration for the UI
                  component
                properties:
                  clientId:
                    description: ClientID is the client ID for authentication
                    type: string
                  kind:
                    description: Kind is the authentication kind (e.g., OIDC)
                    type: string
                  providerLogout:
                    description: ProviderLogout indicates whether to use provider
                      logout
                    type: boolean
                  providerUrl:
                    description: ProviderURL is the URL of the authentication provider
                    type: string
                  redirectUrl:
                    description: RedirectURL is the URL to redirect to after authentication
                    type: string
                  responseType:
                    description: ResponseType is the response type for authentication
                    type: string
                  scopes:
                    description: Scopes are the scopes for authentication
                    type: string
                type: object
              networks:
                description: Networks contains network configuration for the UI component
                  exposer
                properties:
                  host:
                    description: Host is the hostname for the UI component
                    type: string
                  ingressAnnotations:
                    additionalProperties:
                      type: string
                    description: IngressAnnotations contains annotations for the ingress
                    type: object
                  tls:
                    description: TLS contains TLS configuration for the UI component
                    items:
                      description: TLSConfig defines the TLS configuration for the
                        UI component
                      properties:
                        hosts:
                          description: Hosts is a list of hosts included in the TLS
                            certificate
                          items:
                            type: string
                          type: array
                        secretName:
                          description: SecretName is the name of the TLS secret
                          type: string
                      type: object
                    type: array
                type: object
              runtimeAppConfiguration:
                description: RuntimeAppConfiguration contains the runtime configuration
                  for the UI component
                properties:
                  selfConfiguration:
                    description: SelfConfiguration contains the self-configuration
                      for the UI component
                    properties:
                      url:
                        description: URL is the URL of the UI component
                        type: string
                    type: object
                type: object
              scality-ui:
                description: ScalityUI is the name of the ScalityUI resource
                type: string
              scality-ui-component:
                description: ScalityUIComponent is the name of the ScalityUIComponent
                  resource
                type: string
            required:
            - scality-ui
            - scality-ui-component
            type: object
          status:
            description: ScalityUIComponentExposerStatus defines the observed state
              of ScalityUIComponentExposer
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              kind:
                description: Kind is the component kind from the micro-app-configuration
                type: string
              publicPath:
                description: PublicPath is the public path from the micro-app-configuration
                type: string
              version:
                description: Version is the version from the micro-app-configuration
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
